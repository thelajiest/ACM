#include <bits/stdc++.h>
using namespace std;
using i64 = long long;

void solve() {
    int a, b, c, d;
    int e, f, g, h;
    cin >> a >> b >> c >> d;
    cin >> e >> f >> g >> h;
    vector<pair<int, int>> d = {{1, 0}, {-1, 0}, {0, 1}, {0, -1}};
    for (int i = 0; i <= 10; i++)
        for (int j = 0; j <= 10; j++) {
        }
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T;
    cin >> T;
    while (T--) {
        solve();
    }
}
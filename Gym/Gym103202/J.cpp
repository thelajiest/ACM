// don't know
#include <bits/stdc++.h>

using namespace std;
using i64 = long long;

constexpr int block = 5000;

int main() {
    std::ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, q;
    cin >> n >> q;

    return 0;
}